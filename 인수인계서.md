# 인수인계서 — 수업프로그램 허브

> 이 문서 하나면 프로젝트 전체를 파악하고 이어서 작업할 수 있습니다.
> **새 채팅에서 이어서 만들 때는 이 파일을 통째로 붙여넣으면** 매번 처음부터 설명할 필요가 없습니다.

---

## 1. 이게 뭐하는 프로젝트인가

같은 학교 선생님들끼리 **수업프로그램을 모아서 공유하는 사이트**입니다.
관리자가 프로그램(수업)을 올리면, 교사들이 로그인해서 열람·활용하고,
학생들은 계정 없이 참여 코드로 활동 보드에 결과물을 냅니다.

**한 프로그램에 담기는 것:**
- 🔗 수업 링크 (젭·패들렛·AI도구 등 외부 링크)
- 🖥️ 웹앱 (배포된 웹앱 링크, 또는 업로드한 HTML)
- ▶ 영상 (유튜브 임베드)
- 📎 첨부자료 (PPT/PDF/한글/이미지 — 기본 보기전용)
- 📌 학생 활동 보드 (패들렛처럼 학생이 6자리 코드로 들어와 결과물 제출)

## 2. 기술 구성 (한눈에)

- **프레임워크 없음.** 순수 Node `http` 서버 + 바닐라 JS SPA.
- **의존성은 `pg`(Postgres 드라이버) 딱 하나.** 빌드 스텝 없음.
- **DB·파일저장**: Supabase (Postgres + Storage 비공개 버킷)
- **배포**: Vercel (서버리스, 서울 리전 `icn1`)

```
server.js          # 로컬 실행 진입점 (정적파일 + /api)
api/index.js       # Vercel 서버리스 진입점 (같은 handleApi를 씀)
lib/db.js          # DB 스키마·시드·설정·감사로그
lib/api.js         # API 라우트 전체 + 킬스위치 게이트  ← 백엔드 핵심
lib/auth.js        # 세션 (매 요청마다 계정 active 확인 → 즉시 강퇴 가능)
lib/password.js    # scrypt 비밀번호 해시
lib/storage.js     # Supabase Storage 서명 업로드/다운로드
public/            # SPA (index.html + app.js + style.css)  ← 프론트 전체
webapps/           # 허브에 올릴 HTML 교안·웹앱 원본 보관소 (아래 6번)
```

## 3. 데이터 구조 (테이블)

| 테이블 | 내용 |
|---|---|
| `users` | 계정 (role: `admin` / `teacher`) |
| `sessions` | 로그인 세션 (12시간) |
| `programs` | 수업프로그램. `published`(공개여부), `sort_order` |
| `lessons` | 차시 (프로그램 안의 회차). 링크·파일이 차시에 소속 가능 |
| `program_links` | 링크. `kind`: `link`(외부) / `aiapp`(웹앱) / `video`(유튜브) |
| `program_files` | 첨부파일. 실체는 Supabase 버킷. `downloadable`(기본 false=보기전용) |
| `boards` | 학생 활동 보드. `code`(참여코드), `is_open`(마감여부) |
| `board_posts` | 학생 게시물 (계정 없음, 이름 텍스트만) |
| `settings` | 사이트 설정 (킬스위치 등) |
| `audit_logs` | 감사 로그 (모든 주요 행동 기록) |

**관계:** `programs` → `lessons`/`program_links`/`program_files`/`boards` → `board_posts`

## 4. 계정·권한 체계

- **admin (관리자)**: 프로그램·계정·사이트 전부 관리. 첨부 원본 항상 다운로드 가능.
- **teacher (교사)**: 공개된 프로그램 열람. 다운로드는 파일별 허용된 것만.
- **학생**: 계정 없음. 6자리(또는 지정한) 참여 코드로 보드에만 접근.
- 최초 로그인: `superadmin` / 환경변수 `ADMIN_PASSWORD` 값 (미설정 시 `ChangeMe123!`) → 즉시 비번 변경 요구.

## 5. 핵심 설계 — 회수권 (만든 사람이 언제든 즉시 차단)

이 프로젝트에서 제일 신경 쓴 부분. 5개의 즉시 회수 스위치가 있습니다.

| # | 스위치 | 효과 |
|---|---|---|
| 1 | 프로그램별 비공개 | 교사 목록·상세·열람 즉시 차단 |
| 2 | 계정 정지(강퇴) | 세션 즉시 삭제 → 다음 클릭부터 로그아웃, 재로그인 불가 |
| 3 | 사이트 킬스위치 | 관리자 제외 전원 차단 + 안내 문구 |
| 4 | 첨부 원본 소멸 | 파일 삭제 시 Supabase 버킷 객체까지 삭제 — 원본이 사라짐 |
| 5 | 보드 마감 | 학생 제출·열람 즉시 차단 (결과물은 교사·관리자 계속 보관) |

- 다운로드 링크는 **10분 유효 서명 URL**이라 회수 후 기존 링크도 곧 만료됩니다.
- `lib/auth.js`가 **매 요청마다 계정 active를 확인**하므로 정지가 즉시 반영됩니다.

## 6. 콘텐츠(교안·웹앱) 추가하는 법 — 현재 워크플로우

**개별 교안·웹앱마다 레포를 새로 만들지 않습니다.** 방식은 두 가지:

### (A) HTML 웹앱 — 파일 하나를 업로드하면 웹앱처럼 실행됨
허브는 업로드된 `.html`을 새 탭에서 실제 웹앱처럼 실행해 줍니다
(`lib/api.js`의 `/api/files/:id/open` 라우트가 `text/html`로 직접 서빙).

원본은 **`webapps/` 폴더에 보관** → 허브 관리자 화면에서 **첨부로 업로드**.

**HTML 제작 규칙 3가지 (안 지키면 웹앱으로 안 열림):**
1. 파일 하나로 완결 — CSS는 `<style>`, JS는 `<script>`로 안에 직접. 바깥 파일 참조 ❌
2. 3.5MB 이하 (넘으면 다운로드로 빠짐)
3. 백엔드 없음 — 브라우저만으로 도는 것만 (서버·DB 필요하면 아래 B)
- `<meta charset="utf-8">` 필수 (없으면 한글 깨짐)
- 자세한 건 `webapps/README.md`, 뼈대는 `webapps/_template.html`, 예시는 `webapps/examples/모둠뽑기.html`

### (B) 별도 배포 + 링크 — 서버·DB가 필요한 웹앱
Vercel 등에 따로 배포하고 허브에는 `aiapp` 종류의 **링크만** 등록.

### 첨부 파일 업로드가 특이한 이유 (서명 업로드 3단계)
Vercel 응답 한도가 4.5MB라, 파일 실체는 서버를 거치지 않고 브라우저가 Supabase로 직접 보냅니다:
1. 서버가 서명 업로드 URL 발급 (`/file-sign`)
2. 브라우저가 그 URL로 파일 직접 PUT
3. 서버에 완료 확인 (`/file-confirm`) → DB에 등록

## 7. 다운로드·반출 정책

| 방향 | 정책 |
|---|---|
| 관리자 → 교사 (수업자료) | 기본 **보기전용**. 파일별 다운로드 허용 토글 가능. **관리자는 항상 다운로드 가능** |
| 학생 → 교사 (활동 결과물) | 교사 **다운로드 허용** (수합 목적, 원본 파일명 유지) |

→ 올려둔 HTML/웹앱은 **관리자 계정으로 언제든 원본 반출 가능**.
   더 안전하게는 `webapps/` 폴더에 원본을 두어 허브에 종속되지 않게 함.

## 8. 환경변수 (배포 시 필요)

| 변수 | 필수 | 설명 |
|---|---|---|
| `DATABASE_URL` | ✅ | Supabase → Connect → Transaction pooler URI (포트 6543) |
| `ADMIN_PASSWORD` | 권장 | 최초 admin 비밀번호 |
| `SUPABASE_URL` | 첨부 사용 시 | Settings → API의 프로젝트 URL |
| `SUPABASE_SERVICE_KEY` | 첨부 사용 시 | service_role 키 |
| `SUPABASE_BUCKET` | 선택 | 기본 `files` (비공개 버킷, 첫 업로드 시 자동 생성) |

## 9. 실행·배포

```bash
# 로컬 실행
npm install
DATABASE_URL='postgres://...' node --no-warnings server.js   # http://localhost:3000
```

**Vercel 배포:** 레포 연결 → 위 환경변수 입력 → 배포. 빌드 스텝 없음(`vercel.json`이 라우팅 처리).

## 10. 새 채팅에서 이어서 작업할 때 (붙여넣기용 요약)

> 나는 학교 선생님이고, 프레임워크 없는 Node 서버 + 바닐라 JS SPA + Supabase +
> Vercel로 된 "수업프로그램 허브"를 운영 중이다. 의존성은 `pg` 하나뿐이고 빌드
> 스텝이 없다. 백엔드 핵심은 `lib/api.js`, 프론트는 `public/`, DB 스키마는
> `lib/db.js`에 있다. 설계에서 제일 중요한 건 "회수권"(만든 사람이 언제든 즉시
> 차단할 수 있는 5개 스위치)이다. 교안·웹앱은 레포를 늘리지 않고, 자기완결
> HTML 한 파일로 만들어 `webapps/`에 보관한 뒤 허브에 업로드해서 쓴다
> (HTML 규칙: 파일 하나로 완결 / 3.5MB 이하 / 백엔드 없음 / utf-8).

**작업 시 주의:**
- 의존성을 함부로 늘리지 말 것 (`pg` 하나 원칙).
- 회수권 관련 로직(공개여부·세션 active 확인·서명 URL 만료)을 건드릴 땐 특히 신중히.
- 새 기능은 기존 라우트 패턴(`route(method, 정규식, 최소권한, 핸들러)`)을 따를 것.
- 프론트는 SPA 한 덩어리(`public/app.js`)라, 화면 추가 시 기존 구조를 먼저 읽고 맞출 것.

---

*마지막 업데이트: 이 문서는 코드 기준으로 작성됨. 구조가 바뀌면 함께 갱신하세요.*
